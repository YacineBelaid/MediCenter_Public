generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = "mysql://root:root@localhost:3306/Read_DMC"
}

model Patient {
  id             Int  @id @db.UnsignedInt
  nom            Int?
  prenom         Int?
  date_naissance Int?
}
